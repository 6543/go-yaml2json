when:
  - branch: main
    event: push
  - event: pull_request

steps:
  test:
    image: golang:${GO_VERSION}
    commands:
      - go test -race -coverprofile coverage_${GO_VERSION}.out ./...

matrix:
  GO_VERSION:
    - 1.22
    - 1.24
    - 1.26
